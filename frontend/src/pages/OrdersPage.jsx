import { useEffect, useState } from 'react'
import commerceService from '../services/commerceService'
import '../styles/portal.css'

export function OrdersPage() { const [orders, setOrders] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { commerceService.getOrders().then((data) => setOrders(data?.items || data || [])).finally(() => setLoading(false)) }, []); return <div className="orders-page"><div className="page-heading"><span className="eyebrow">YOUR JOURNEY</span><h1>My orders</h1><p>Every order, from placed to delivered.</p></div>{loading ? <div className="page-state">Loading your orders...</div> : orders.length ? <div className="customer-orders">{orders.map((order) => <article key={order.id}><div><span className="eyebrow">ORDER #{order.id}</span><h2>{order.status || 'Processing'}</h2><small>{order.createdAt ? new Date(order.createdAt).toLocaleDateString() : 'Recently placed'}</small></div><strong>${Number(order.total || order.totalAmount || 0).toFixed(2)}</strong><span className={`order-status ${String(order.status).toLowerCase()}`}>{order.status || 'pending'}</span></article>)}</div> : <div className="empty-panel"><span>≡</span><h2>No orders yet</h2><p>Your next favorite find is waiting in the shop.</p></div>}</div> }
